module.exports = {
    project: {
      ios: {},
      android: {},
    },
    assets: ['./assets/fonts'], // adjust according to your path
  };

  // Active, background , kill
  // rn doenst allow the js bundler to complete the application to completly kill the app
  // Component api name to check the state ->App state
// Dimensions...